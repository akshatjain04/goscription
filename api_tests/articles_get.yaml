# Test generated by RoostGPT for test goscription using AI Type Open AI and AI Model gpt-4

# RoostTestHash=08a1b138b1

config:
  target: "{{ $processEnvironment.API_HOST }}"
  phases:
    - duration: 60
      arrivalRate: 20
  plugins:
    expect: {}
  payload:
    path: "articles_get.csv"
    fields:
      - "num"
      - "cursor"
    skipHeader: true

scenarios:
  - name: "Successful API request"
    flow:
      - get:
          url: "/articles?num={{ num }}&cursor={{ cursor }}"
          headers:
            Authorization: "{{ $processEnvironment.AUTH_TOKEN }}"
          expect:
            - statusCode: 200
            - hasProperty: "message"

  - name: "API request with validation failure"
    flow:
      - get:
          url: "/articles?num=&cursor="
          headers:
            Authorization: "{{ $processEnvironment.AUTH_TOKEN }}"
          expect:
            - statusCode: 400
            - hasProperty: "message"

  - name: "API request with edge case"
    flow:
      - get:
          url: "/articles?num=*&cursor=*&"
          headers:
            Authorization: "{{ $processEnvironment.AUTH_TOKEN }}"
          expect:
            - statusCode: 400
            - hasProperty: "message"

  - name: "API request with 404 status"
    flow:
      - get:
          url: "/non-existent-endpoint"
          headers:
            Authorization: "{{ $processEnvironment.AUTH_TOKEN }}"
          expect:
            - statusCode: 404
            - hasProperty: "message"

  - name: "API request with 500 status"
    flow:
      - get:
          url: "/articles?num=*&cursor=*&"
          headers:
            Authorization: "{{ $processEnvironment.AUTH_TOKEN }}"
          expect:
            - statusCode: 500
            - hasProperty: "message"

  - name: "API request without authentication"
    flow:
      - get:
          url: "/articles?num={{ num }}&cursor={{ cursor }}"
          expect:
            - statusCode: 401
            - hasProperty: "message"

  - name: "API request with invalid authentication credentials"
    flow:
      - get:
          url: "/articles?num={{ num }}&cursor={{ cursor }}"
          headers:
            Authorization: "InvalidToken"
          expect:
            - statusCode: 401
            - hasProperty: "message"
