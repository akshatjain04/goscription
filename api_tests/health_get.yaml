# Test generated by RoostGPT for test goscription using AI Type Open AI and AI Model gpt-4

# RoostTestHash=513a69c9fa

config:
  target: "{{ $processEnvironment.API_HOST }}"
  phases:
    - duration: 60
      arrivalRate: 20
  plugins:
    expect: {}
scenarios:
  - name: "Successful API request"
    flow:
      - get:
          url: "/health"
          expect:
            - statusCode: 200
  - name: "API request with validation failure"
    flow:
      - get:
          url: "/invalid_url"
          expect:
            - statusCode: 400
  - name: "API request with edge case"
    flow:
      - get:
          url: "/health"
          headers:
            Content-Type: "application/x-www-form-urlencoded"
          expect:
            - statusCode: 415
  - name: "API request with 404 status code"
    flow:
      - get:
          url: "/non_existent_path"
          expect:
            - statusCode: 404
  - name: "API request with 500 status code"
    flow:
      - get:
          url: "/health"
          beforeRequest: "simulateServerError"
          expect:
            - statusCode: 500
  - name: "API request without authentication"
    flow:
      - get:
          url: "/health"
          headers:
            Authorization: ""
          expect:
            - statusCode: 401
  - name: "API request with invalid authentication credentials"
    flow:
      - get:
          url: "/health"
          headers:
            Authorization: "Invalid {{ $processEnvironment.AUTH_TOKEN }}"
          expect:
            - statusCode: 403
  - name: "API request with insufficient permissions"
    flow:
      - get:
          url: "/health"
          headers:
            Authorization: "{{ $processEnvironment.LOW_PRIVILEGE_AUTH_TOKEN }}"
          expect:
            - statusCode: 403
  - name: "API request when service/database is unavailable"
    flow:
      - get:
          url: "/health"
          beforeRequest: "simulateServiceUnavailable"
          expect:
            - statusCode: 503
  - name: "API request with unsupported HTTP method"
    flow:
      - post:
          url: "/health"
          expect:
            - statusCode: 405
  - name: "API request with malformed headers"
    flow:
      - get:
          url: "/health"
          headers:
            Content-Type: "malformed"
          expect:
            - statusCode: 400
