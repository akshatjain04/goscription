# Test generated by RoostGPT for test goscription using AI Type Open AI and AI Model gpt-4

config:
  target: "{{ $processEnvironment.SERVER_URL }}"
  plugins:
    expect: {}
scenarios:
  - name: "Successful API request"
    flow:
      - get:
          url: "/health"
          expect:
            - statusCode: 200
            - hasProperty: "success"
            - hasProperty: "message"

  - name: "API request with validation failure"
    flow:
      - get:
          url: "/invalidURL"
          expect:
            - statusCode: 400
            - hasProperty: "error"

  - name: "API request with edge case"
    flow:
      - get:
          url: "/health"
          headers:
            Content-Type: "application/json; charset=x-user-defined"
          expect:
            - statusCode: 200
            - hasProperty: "success"
            - hasProperty: "message"
