# Test generated by RoostGPT for test goscription using AI Type Open AI and AI Model gpt-4

# RoostTestHash=ca3b71d213

config:
  target: "{{ $processEnvironment.API_HOST }}"
  phases:
    - duration: 60
      arrivalRate: 20
  plugins:
    expect: {}
scenarios:
  - name: "Successful API request"
    flow:
      - get:
          url: "/address"
          headers:
            Authorization: "{{ $processEnvironment.AUTH_TOKEN }}"
          expect:
            - statusCode: 200
            - hasProperty: "data"
  - name: "API request with validation failure"
    flow:
      - get:
          url: "/invalidurl"
          headers:
            Authorization: "{{ $processEnvironment.AUTH_TOKEN }}"
          expect:
            - statusCode: 400
            - hasProperty: "error"
            - hasProperty: "message"
  - name: "API request with edge case"
    flow:
      - get:
          url: "/address?name=*&^%$#@!"
          headers:
            Authorization: "{{ $processEnvironment.AUTH_TOKEN }}"
          expect:
            - statusCode: 200
            - hasProperty: "data"
  - name: "API request with 404 status"
    flow:
      - get:
          url: "/nonexistent"
          headers:
            Authorization: "{{ $processEnvironment.AUTH_TOKEN }}"
          expect:
            - statusCode: 404
            - hasProperty: "error"
            - hasProperty: "message"
  - name: "API request with 500 status"
    flow:
      - get:
          url: "/address"
          headers:
            Authorization: "Invalid token"
          expect:
            - statusCode: 500
            - hasProperty: "error"
            - hasProperty: "message"
